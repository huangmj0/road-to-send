interface:
  display_name: "Road to Send Drain"
  short_description: "Run one reviewed queue or ship tick"
  default_prompt: "Use $road-to-send-drain to run one serialized implementation or batch-release tick."
policy:
  allow_implicit_invocation: false
